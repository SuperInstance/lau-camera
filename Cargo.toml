[package]
name = "lau-camera"
version = "0.1.0"
edition = "2021"
description = "2D camera system for games — viewport, zoom, follow, shake, smooth transitions"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
